apiVersion: v1
kind: Service
metadata:
  name: corebanking-ledger
  namespace: corebanking
  labels:
    app: corebanking-ledger
spec:
  type: ClusterIP
  selector:
    app: corebanking-ledger
  ports:
    - name: http
      port: 80
      targetPort: http
